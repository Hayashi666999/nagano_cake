
  		 <% if @books.errors.any? %>
  <%= @books.errors.count %>error prohibited this obj from being saved:
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  		<%= form_with model: @books, url: book_path(@books.id), method: :patch do |f| %>

  <h4>Title</h4>
  <%= f.text_field :name %>
  
  <tb><%= f.submit 'Update Book',class:"btn btn-success" %></tb>
  
<% end %>